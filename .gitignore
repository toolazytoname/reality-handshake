# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local overrides (if anyone copies skill for hacking)
CLAUDE.md
.claude/settings.local.json

# Sensitive — never commit
*.pem
*.key
.env
.env.local

# Build / test artifacts (if we add them later)
dist/
build/
*.test
coverage/
